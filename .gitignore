/.phpunit.cache
/bootstrap/ssr
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
# Filament publishes its compiled assets here; regenerated on every composer
# install via the `filament:upgrade` script (so deploys stay in sync).
/public/css/filament
/public/js/filament
/public/fonts/filament
/storage/*.key
/storage/pail
/resources/js/actions
/resources/js/routes
/resources/js/wayfinder
/vendor
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
# Long-lived Facebook / Instagram Graph API tokens — never commit these.
/facebook-kit/*token*.txt
# Vendor prospecting list (compiled business contact info). This repo is PUBLIC —
# publishing an aggregated contact list is needless CASL exposure and hands
# competitors the recruiting targets. Keep it local.
/vendor-targets.csv
/.fleet
/.idea
/.nova
/.vscode
/.zed
